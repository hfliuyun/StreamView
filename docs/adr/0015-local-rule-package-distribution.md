# Local Rule Package Distribution

Official rules will ship with the application, while additional rule packages may be installed from local files or directories. The first release has no marketplace, runtime dependency download, or automatic rule update. Saved sessions pin the exact selected rule version, old versions may be retained for reproducibility, and every installed rule remains inside the same sandbox regardless of claimed provenance. This establishes an extensible offline workflow without adding a remote supply chain to the initial product.
